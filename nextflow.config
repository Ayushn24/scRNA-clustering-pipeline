process {
  container = 'scrna_docker_image' // Set this to the name of your loaded image
  executor = 'local' // Keep your executor setting
}

docker {
  enabled = true
  pullLatest = true // This is good practice to ensure Nextflow always checks for the latest version of the image
}